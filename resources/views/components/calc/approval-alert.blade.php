<div role="alert" class="alert alert-warning">
    <i class="bi bi-exclamation-triangle-fill text-lg"></i>
    <div>
        <div class="font-semibold">{{ __('Market Approval Required') }}</div>
        <div class="text-sm">{{ __('Sales price is more than 5% below the budget price. This quote requires market approval before submission.') }}</div>
    </div>
</div>
